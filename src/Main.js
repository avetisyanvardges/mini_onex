import React, { Fragment } from 'react';
import StackNavigation from 'navigation/StackNavigation';

const Main = () => {
  return (
    <Fragment>
      <StackNavigation />
    </Fragment>
  );
};

export default Main;
